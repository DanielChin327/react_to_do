# config.py

# Database configuration dictionary to store MySQL connection settings
DB_CONFIG = {
    'host': 'localhost',       # MySQL server is hosted locally
    'user': 'root',            # Default root user for MySQL
    'password': '',            # Password for the MySQL root user (leave empty if not set)
    'database': 'react_to_do'  # Name of the database created in XAMPP
}
